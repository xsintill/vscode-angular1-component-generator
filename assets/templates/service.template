namespace {namespace} {

    export const {serviceNameConstantCase}_SERVICE_NAME: string = "{serviceName}Service";

   /**
    *   Controller for service {serviceName}
    */
    export class {serviceName}Service {
        public static $inject: string[] = [
        ];

        constructor() {
        }
    }
    angular.module(Application.LLQAPP_NAME).service({serviceNameConstantCase}_SERVICE_NAME, {serviceName}Service);
}
